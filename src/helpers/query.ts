import { request } from 'graphql-request';

export const query = async ({
    endpoint,
    variables,
    query,
}: {
    endpoint: string,
    variables: string,
    query: string,
}) => {
    try {

    } catch (err) {

    };
};